spring.application.name=inspection-api

# Database Connection (PostgreSQL)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/inspection_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:julie}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:jsB200244}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false
